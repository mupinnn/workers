{
  "name": "workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.7.11"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "wrangler": "^4.4.0"
  }
}

